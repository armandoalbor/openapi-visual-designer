import { Box, Typography } from "@mui/material";

export const ValidatorPage = () => {
  return (
    <Box
      display="flex"
      height="100%"
      justifyContent="center"
      alignItems="center"
    >
      <Typography variant="h1" textTransform="capitalize">
        Welcome to ValidatorPage
      </Typography>
    </Box>
  );
};
